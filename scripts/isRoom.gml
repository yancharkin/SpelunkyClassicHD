//
// isRoom(name)
//
// Check current room with name (e.g. "rLevel")
//

if (room_get_name(room) == argument0) return true;
else if argument0="rTutorial" && room=rTutorial return true;
else return false;
