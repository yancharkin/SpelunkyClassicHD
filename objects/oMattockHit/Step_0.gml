if (instance_number(oCharacter) == 0)
{
    instance_destroy();
}
if (sprite_index = sMattockHitR)
{
    x = oCharacter.x+16;
    y = oCharacter.y;
}
else if (sprite_index = sMattockHitL)
{
    x = oCharacter.x-16;
    y = oCharacter.y;
}

